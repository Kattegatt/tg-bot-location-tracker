import { describe, it } from "vitest";

describe("points integration", () => {
  it("creates, refreshes, fetches", async () => {
    // TODO: spin up test DB, call /points/location twice, assert merge + TTL.
  });
});
